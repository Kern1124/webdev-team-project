import { Routes, Route } from "react-router-dom";
import { Box } from "@chakra-ui/react";

function App() {
  return (
    <Box padding="1rem">
      <Routes>
        <Route path="/" />
      </Routes>
    </Box>
  );
}

export default App;
